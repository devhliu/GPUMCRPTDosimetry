# Decay DB Import (Deprecated)

This repository no longer uses CSV/YAML-based decay imports.

## Current approach
Use **local ICRP107 JSON nuclide files** in `data/decaydb/icrp107/` as the authoritative decay database.

See:
- `docs/14_decaydb_icrp107_json_only.md`